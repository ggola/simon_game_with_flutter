import 'package:flutter/material.dart';
import 'package:simon_game/Widgets/gesture_detector.dart';

class StartScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: GestureDetectorWidget(),
    );
  }
}
